import { InstanceFilterPipe } from './instance-filter.pipe';

describe('InstanceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InstanceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
